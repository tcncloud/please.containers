subinclude('//:testing')

foobar(
    name = "foo",
    files = {
        'foo': '/path/to/bar'
    }
)

foobar(
    name = "bar",
    files = {
        'bar': '/path/to/foo:755:root:root',
    }
)

foobar(
    name = "baz",
    files = {
        'baz': '/path/to/baz:755:root',
    }
)